# Cloudflare API token with proper permissions
CF_AUTH_TOKEN=""

# Zone ID for the domain
ZONE_ID=""

# Ruleset and Rule IDs
RULESET_ID=""
RULE_ID=""

# Static IPs that should always remain in the allowlist
FIXED_IPS=("")
